import { ClipboardList, BookOpen, Radar, type LucideIcon } from "lucide-react";

/** Guided BFSG scan + fix plan workflow */
export const A11yFixIcon: LucideIcon = ClipboardList;

/** POUR "Understandable" principle */
export const UnderstandableIcon: LucideIcon = BookOpen;

/** Free snapshot / scan tier */
export const SnapshotScanIcon: LucideIcon = Radar;
